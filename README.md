# 🌌 Laniakea Protocol - پروتوکل لانیاکیا

**نسخه**: v0.0.1 (Alpha) | **وضعیت**: ارگانیسم محاسباتی خودتوسعه‌دهنده

---

پروتوکل Laniakea یک **ارگانیسم محاسباتی کیهانی** است که برای حل مسائل واقعی جهانی طراحی شده است. این پروتوکل با الهام از ساختار ابرخوشه کهکشانی لانیاکیا، به دنبال ایجاد یک شبکه غیرمتمرکز از هوش و محاسبات است که به جای استخراج بی‌هدف، انرژی خود را صرف **تولید دانش، آگاهی و راه‌حل‌های پایدار** می‌کند.

## ✨ ویژگی‌های کلیدی (v0.0.1)

| ویژگی | توضیحات | ماژول‌های مرتبط |
| :--- | :--- | :--- |
| **Proof of Value (PoV)** | الگوریتم اجماع جدید که به جای قدرت محاسباتی، به راه‌حل‌ها بر اساس ارزش چندبعدی آن‌ها پاداش می‌دهد. | `consensus`, `core` |
| **ارزش‌گذاری چند بُعدی** | ارزیابی راه‌حل‌ها بر اساس ابعاد: **دانش، محاسبات، خلاقیت، آگاهی، محیط زیست و سلامت**. | `core/models` |
| **هسته شناختی (Cognitive Core)** | مغز مرکزی پروژه، مجهز به **API هوش مصنوعی (LLM)** برای تحلیل بلاک‌ها، ارزیابی راه‌حل‌ها و تولید خودکار تسک‌های جدید. | `metasystem`, `intelligence` |
| **بازار دانش (Knowledge Market)** | پلتفرمی برای تبدیل دانش، کشفیات و راه‌حل‌ها به **دارایی‌های دانشی (Knowledge Assets)** قابل معامله و مالکیت. | `marketplace` |
| **سیستم اعتبار (Reputation System)** | یک سیستم پیشرفته برای محاسبه اعتبار نودها بر اساس کیفیت و کمیت مشارکت‌ها، تضمین‌کننده امنیت و کیفیت شبکه. | `reputation` |
| **یکپارچگی API خارجی** | اتصال مستقیم به منابع داده کیهانی و زمینی (NASA, Wolfram Alpha, Financial, Weather) برای غنی‌سازی تسک‌ها و راه‌حل‌ها. | `external_apis` |
| **رابط کاربری وب (Web UI)** | یک داشبورد وب کامل برای مشاهده وضعیت شبکه، مدیریت تسک‌ها و تعامل با هسته شناختی. | `web` |

## 🚀 شروع به کار

### ۱. پیش‌نیازها

- Python 3.11+
- `pip`

### ۲. نصب وابستگی‌ها

```bash
# ایجاد و فعال‌سازی محیط مجازی (توصیه می‌شود)
python3 -m venv venv
source venv/bin/activate

# نصب وابستگی‌ها
pip install -r requirements.txt
```

### ۳. تنظیم متغیرهای محیطی

برای فعال‌سازی کامل هسته شناختی و APIهای خارجی، کلیدهای API خود را در یک فایل `.env` در ریشه پروژه قرار دهید:

```
# برای هسته شناختی (Cognitive Core)
OPENAI_API_KEY="sk-your-openai-key-here"

# برای APIهای خارجی (اختیاری)
NASA_API_KEY="your-nasa-key-here"
OPENWEATHER_API_KEY="your-weather-key-here"
ALPHAVANTAGE_API_KEY="your-financial-key-here"
WOLFRAM_APP_ID="your-wolfram-id-here"
```

### ۴. اجرای نود

از اسکریپت راه‌اندازی برای اجرای نود استفاده کنید:

```bash
# راه‌اندازی با پورت‌های پیش‌فرض (P2P: 5000, API: 8000)
./start_node.sh

# برای راه‌اندازی با شبیه‌ساز کیهانی
./start_node.sh 5000 8000 --enable-simulation
```

### ۵. دسترسی به رابط کاربری

پس از اجرای نود، داشبورد وب را در مرورگر خود باز کنید:

[http://localhost:8000/ui](http://localhost:8000/ui)

## 🛠️ توسعه درونی دائمی (Self-Evolution Loop)

این پروژه برای **توسعه خودکار و دائمی** طراحی شده است. هسته شناختی به طور مداوم کدها را تحلیل می‌کند، نقاط ضعف را شناسایی می‌کند و پیشنهادهای بهبود ارائه می‌دهد.

**برای فعال‌سازی این چرخه توسعه درونی دائمی:**

1.  **اطمینان از دسترسی گیت‌هاب**: مطمئن شوید که نود در محیطی با دسترسی کامل به گیت‌هاب (با توکن توسعه) اجرا می‌شود.
2.  **اجرای اسکریپت توسعه**: در فاز بعدی، یک اسکریپت مجزا برای فعال‌سازی چرخه توسعه خودکار ارائه خواهد شد که به صورت دوره‌ای تغییرات را به مخزن شما ارسال می‌کند.

## 🤝 مشارکت

پروتوکل Laniakea یک پروژه جامعه‌محور است. مشارکت شما در هر یک از زمینه‌های زیر ارزشمند است.

---

**© 2025 Laniakea Protocol** - جایی که دانش، آگاهی و محاسبات همگرا می‌شوند.
